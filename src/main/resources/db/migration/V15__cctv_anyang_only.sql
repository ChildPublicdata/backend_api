-- [이 파일이 왜 필요한가]
-- 이 프로젝트의 다른 모든 도메인(SafeZone/RiskZone/GridRisk/Facility)은 경기도 안양시 하나만 다루는데,
-- CCTV만 V8에서 대전광역시 서구(812건) + 경기도 안양시(1749건)를 섞어서 내려주고 있었음.
-- 대전서구 데이터는 이 프로젝트 어디에서도 쓰이지 않는 죽은 데이터라 API에서 걷어내기로 함.
--
-- [규칙] 이미 서버에 적용된 마이그레이션(V1~V14)은 절대 수정하지 말 것. 새 번호(V15)로 파일을 추가함.
--
-- cctv.json에서 city="대전광역시 서구" 행을 제거해 안양시(1749건)만 남겨뒀고, DataSeeder는 "테이블에
-- 행이 있으면 적재를 건너뛴다"는 규칙이라 여기서 비워주지 않으면 기존 2561건(대전서구 포함)이 남아있는
-- 환경(로컬/Railway)에서 안양시 단독 데이터가 영영 반영되지 않음.
delete from cctv;
